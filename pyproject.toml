[project]
name = "leeds-meetup-dashboard"
version = "1.0.0"
requires-python = ">=3.12"
dependencies = [
    "requests==2.32.3",
    "icalendar==6.1.3",
]

[tool.vercel]
entrypoint = "api/sync:handler"
